# Assets

Runtime resources for the Blender extension live here.

- `icons/`: custom UI preview icons.
- `images/readme/`: images referenced by the repository README.
- `scenes/asset_helper/`: `.blend` scenes and thumbnails used by asset helper preview rendering.
- `scripts/`: Blender background scripts invoked by operators.
- `templates/`: reusable `.blend` templates loaded by operators.
